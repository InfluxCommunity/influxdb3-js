// grpc implementation for browser not present
// to build browser, replace build:browser in client with
// "build:browser": "yarn yarn tsup --config ./tsup.config.browser.ts"
console.warn('browser currently no supported')
